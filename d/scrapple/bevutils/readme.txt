To build bevutils.lib, more specifically log4d.d, you will need to rebuild
phobos.lib with the the files following, and included with the bevutils
file set.

date.d - includes function listthisdir (non recursive).
date.d - includes a function to populate Date struct from ticks.
