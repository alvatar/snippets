module tools.unittests;

import tools.all;

void main() {
  logln("Unittests finished",
    passed?Format(", ", passed, "/", passed+failed, " passed"):"",
    failed?Format(", ", failed, "/", passed+failed, " failed"):""
  );
}
